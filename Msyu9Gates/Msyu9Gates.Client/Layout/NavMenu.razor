<div class="top-row ps-3 navbar navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">9 Gates</a>
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<div class="nav-scrollable bg-dark" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="nav flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span aria-hidden="true"></span> 0
            </NavLink>
        </div>

        @for (int i = 1; i <= 9; i++)
        {
            int gateNumber = i;
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="@($"gate/{gateNumber}")">
                    <span aria-hidden="true"></span> <strong>@GateUtils.ToRoman(gateNumber)</strong>
                </NavLink>
            </div>
        }
    </nav>
</div>

@code {
    
}
