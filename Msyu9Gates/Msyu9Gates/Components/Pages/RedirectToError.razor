@inject NavigationManager Navigation

@code {
    [Parameter] public string StatusCode { get; set; } = "404";

    protected override void OnInitialized()
        => Navigation.NavigateTo($"/Error/{StatusCode}", replace: true);
}